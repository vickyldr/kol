[project]
name = "kol-audit"
version = "0.1.0"
description = "KOL 付款审批自动核对引擎"
requires-python = ">=3.9"
dependencies = ["anthropic>=0.49", "pydantic>=2.6"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
